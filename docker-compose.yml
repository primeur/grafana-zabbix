services:
  grafana:
    user: "0:0"
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana

  frontend:
    image: node:24
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "corepack enable && yarn install && yarn build --watch"
